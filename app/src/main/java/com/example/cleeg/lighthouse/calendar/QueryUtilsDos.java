package com.example.cleeg.lighthouse.calendar;

public class QueryUtilsDos {

    // Tag for the log messages
    private static final String TAG = "QueryUtilsDos";

    /**
     * Create a private constructor because no one should ever create a {@link QueryUtilsDos} object.
     * This class is only meant to hold static variables and methods, which can be accessed
     * directly from the class name QueryUtils (and an object instance of QueryUtils is not needed).
     */
    private QueryUtilsDos() {}
}
